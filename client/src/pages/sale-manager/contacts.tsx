import { ManagerContactTable } from '@components/sale-manager/contact/table';

interface ManagerContactProps {}

const ManagerContact: React.FC<ManagerContactProps> = ({}) => {
  return (
    <div>
      <ManagerContactTable />
    </div>
  );
};

export default ManagerContact;
