import { Pipeline } from '@components/pipelines/pipeline';

const Pipelines: React.FC = () => {
  return (
    <>
      <Pipeline />
      {/* <PipelineList /> */}
    </>
  );
};

export default Pipelines;
